# GTAP_convert errors when invalid origin

    `origin` must be one of "GTAPv6" or "GTAPv7", not "GTAPv8".
    i Did you mean "GTAPv6"?

# GTAP_convert errors when invalid target

    `target` must be one of "GTAPv6" or "GTAPv7", not "GTAPv3".
    i Did you mean "GTAPv6"?

# GTAP_convert errors when incorrect origin

    x `target` set to GTAPv6 but data appears to already be this format.

# GTAP_convert errors when origin equals target

    x `target` must differ from `origin`.

