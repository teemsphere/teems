# ems_example errors when model is missing

    x argument `model` is missing, with no default

# ems_example errors when write_dir does not exist

    x The path provided for `write_dir`, '/home/mpc/.cache/R/teems/nonexistent_dir_xyz', does not exist.

# ems_example errors when type is scripts and dat_input is missing

    x `dat_input`, `par_input`, and `set_input` must be provided when `type` is "scripts".

# ems_example errors when type is scripts and par_input is missing

    x `dat_input`, `par_input`, and `set_input` must be provided when `type` is "scripts".

# ems_example errors when type is scripts and set_input is missing

    x `dat_input`, `par_input`, and `set_input` must be provided when `type` is "scripts".

