% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ems_deploy.R
\name{ems_deploy}
\alias{ems_deploy}
\title{Consolidate and write all model inputs}
\usage{
ems_deploy(
  .data,
  model,
  shock = NULL,
  swap_in = NULL,
  swap_out = NULL,
  write_dir = tools::R_user_dir("teems", "cache"),
  shock_file = NULL
)
}
\arguments{
\item{.data}{A list of data.tables generated by
\code{\link[=ems_data]{ems_data()}}.}

\item{model}{A tibble generated by \code{\link[=ems_model]{ems_model()}}.}

\item{shock}{A \code{shock} object or list of \code{shock} objects
produced by \code{\link[=ems_uniform_shock]{ems_uniform_shock()}}, \code{\link[=ems_custom_shock]{ems_custom_shock()}},
or \code{\link[=ems_scenario_shock]{ems_scenario_shock()}}. When \code{NULL} (default), no shock
is applied and base data is returned.}

\item{swap_in}{A variable name string, \code{\link[=ems_swap]{ems_swap()}} object,
or list of any combination thereof. Variables to be made
exogenous (default \code{NULL}).}

\item{swap_out}{A variable name string, \code{\link[=ems_swap]{ems_swap()}} object,
or list of any combination thereof. Variables to be made
endogenous (default \code{NULL}).}

\item{write_dir}{Path to the base directory where a \verb{teems/}
subdirectory will be created for input files and solver
output. Defaults to a session-scoped cache cleared on exit;
specify a path for persistent output.}

\item{shock_file}{Path to a file with ".shf" extension
representing a fully prepared shock file (default \code{NULL}).
No checks or modifications are applied to this file.}
}
\value{
Path to the CMF file, passed to \code{\link[=ems_solve]{ems_solve()}}.
}
\description{
Validates all data and model inputs, processes shocks and
closure swaps, and writes the input files required by
\code{\link[=ems_solve]{ems_solve()}}.
}
\examples{
\dontrun{
# Uniform shock with a full variable closure swap
shock <- ems_uniform_shock("qfd", value = 1)

cmf_path <- ems_deploy(
  .data = dat,
  model = model,
  shock = shock,
  swap_in = "qfd",
  swap_out = "tfd",
  write_dir = "~/my_project/output"
)

# Uniform shock with multiple heterogeneous swaps 
yp_row <- ems_swap("yp", REGr = "row")
dppriv_row <- ems_swap("dppriv", REGr = "row")


cmf_path <- ems_deploy(
 .data = dat,
 model = model,
 shock = shock,
 swap_in = list(yp_row, "qfd"),
 swap_out = list(dppriv_row, "tfd")
)
}

}
\seealso{
\code{\link[=ems_data]{ems_data()}}. \code{\link[=ems_model]{ems_model()}}.
\code{\link[=ems_uniform_shock]{ems_uniform_shock()}}. \code{\link[=ems_custom_shock]{ems_custom_shock()}}.
\code{\link[=ems_scenario_shock]{ems_scenario_shock()}}. \code{\link[=ems_swap]{ems_swap()}}. \code{\link[=ems_solve]{ems_solve()}}.
}
